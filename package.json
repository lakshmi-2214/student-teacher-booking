{
  "name": "student-teacher-booking",
  "version": "1.0.0",
  "description": "Student Teacher Booking System",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html assets dist/",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "firebase": "^8.10.0"
  },
  "devDependencies": {
    "firebase-tools": "^12.8.0"
  }
}